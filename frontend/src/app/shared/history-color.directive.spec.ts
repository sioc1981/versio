import { HistoryColorDirective } from './history-color.directive';

describe('HistoryColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HistoryColorDirective();
    expect(directive).toBeTruthy();
  });
});
