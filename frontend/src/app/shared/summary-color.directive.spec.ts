import { SummaryColorDirective } from './summary-color.directive';

describe('SummaryColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new SummaryColorDirective();
    // expect(directive).toBeTruthy();
  });
});
